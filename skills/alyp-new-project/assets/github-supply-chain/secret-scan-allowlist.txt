# Allowlist para .github/workflows/secret-scan.yml
# Una subcadena literal por línea. Una línea escaneada que CONTENGA cualquiera
# de estas subcadenas se permite aunque matchee un patrón.
#
# Usar SOLO para falsos positivos legítimos (claves públicas, fixtures de test,
# el anon key público de Supabase si aparece en código de cliente).
# NUNCA agregues un secreto real acá: rotá el secreto y arreglá la fuente.

# Anon key público de Supabase (es seguro en el cliente; NEXT_PUBLIC_*):
NEXT_PUBLIC_SUPABASE_ANON_KEY
